# Strings
## Examples
```
var str = "hell"

# Reassign extended string
var str = str + "o"  # "hello"

# Reassign repeated string
var str = str * 2  # "hellohello"

# Check if type is string
isString(str)  # 1

# Check if empty (falsy or truthy)
isTrue(str)  # 0
```
